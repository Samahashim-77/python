from tkinter import *
from tkinter import messagebox
window3 = Tk()
window3.geometry('300x300')
def msg():
    messagebox.showwarning("Alert", "Stop! Virus Found.")
    button= Button(window3, text="Scan for Virus", command=msg)
    button.place(x=40, y=80)
    window3.mainloop()
